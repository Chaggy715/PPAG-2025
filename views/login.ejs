<%- include('partials/header', {tituloWeb: 'Login'}) %>
<body>
    <link rel="preload" href="css/login.css" as="style"> <!-- precargar plantillas -->
    <link rel="stylesheet" href="/css/login.css?v=1"> <!-- El ?v=1 es para evitar caché -->
<div class="container">
   <div class="">
    <h1>¡Bienvenido!</h1>
    <!-- Mostrar mensaje de error si hay uno -->
        <% if (typeof errorMessage !== 'undefined') { %>
            <div style="color: red; text-align: center;">
                <%= errorMessage %>
            </div>
        <% } %>
    <form action="/login" method="POST">
        <label for="username">Correo:</label>
        <input type="text" id="username" name="username" required><br><br>

        <label for="password">Contraseña:</label>
        <input type="password" id="password" name="password" required><br><br>

        <button type="submit">Inicia Sesión</button>
        <!-- Botón de Login -->
            <section class="start-button">
                <button onclick="window.location.href='/r';">REGÍSTRATE</button>
            </section>
    </form>
   </div>
</div>
</body>
</html>